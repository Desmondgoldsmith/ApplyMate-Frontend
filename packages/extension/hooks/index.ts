/**
 * Extension-local hooks (e.g. `chrome.runtime` messaging wrappers).
 *
 * Prefer importing shared hooks from the workspace package:
 *   import { ... } from '@applymate/shared/hooks/...';
 *
 * Re-export shared hooks here only when an extension-specific adapter is needed.
 */
export {};
