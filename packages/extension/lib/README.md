# `lib/`

| File        | Purpose |
| ----------- | ------- |
| `storage.ts` | `chrome.storage.local` helpers for auth token / prefs. |
| `api.ts`    | Axios client for backend calls (`VITE_API_URL`). |
