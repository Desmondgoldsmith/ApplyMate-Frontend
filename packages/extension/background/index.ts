/**
 * Service worker (Manifest V3). Use for alarms, messaging, and install hooks.
 * Keep this file small; heavy work belongs in offscreen docs or the backend.
 */
chrome.runtime.onInstalled.addListener(() => {
  // Scaffold: wire version bumps, default storage, or onboarding tabs here.
});
