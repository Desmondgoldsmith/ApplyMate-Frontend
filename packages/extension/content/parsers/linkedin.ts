/**
 * LinkedIn-specific DOM parsing. Return structured job fields for the ApplyMate pipeline.
 * Implement selectors against LinkedIn’s markup here — keep parsers isolated per host.
 */
export function parseLinkedInJobPage(): null {
  return null;
}
