/** Shown when `diffSection` is set but per-field `changedFields` is empty. */
export const CV_DIFF_EMPTY_PREVIEW_MESSAGE =
  'AI suggested improvements. Preview not available, but changes can still be applied.';

/** When the server sent structured before/after but no per-field rows yet — not the legacy empty state. */
export const CV_DIFF_STRUCTURAL_SECTION_MESSAGE =
  'AI drafted changes for this section. Use Accept all to apply or Reject all to dismiss.';
