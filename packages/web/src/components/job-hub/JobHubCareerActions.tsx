'use client';

/**
 * Legacy career actions block — no longer mounted in Job Hub detail header.
 * Share / verify CTAs live on JobHubDetailPanel when stage === 'accepted'.
 */
export function JobHubCareerActions() {
  return null;
}
