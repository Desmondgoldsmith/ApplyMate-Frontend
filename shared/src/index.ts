/**
 * @applymate/shared — cross-package components, hooks, types, and utilities.
 * Import from workspace consumers: `import { cn, type User } from '@applymate/shared';`
 */
export { cn } from './lib/utils';
export type { User } from './types/user';
