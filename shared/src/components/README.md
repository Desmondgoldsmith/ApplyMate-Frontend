# `components/`

Presentational building blocks used by **both** the Next.js app and the browser extension (e.g. buttons, typography wrappers).

Keep packages free of Next.js-only APIs here; use `packages/web/src/components` for App Router–specific shells.
